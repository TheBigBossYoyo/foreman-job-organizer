# Copy to .env and fill in. Never commit the real .env.
# Defaults to Anthropic Claude; leave the key blank to run the rule-based demo.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
# Force a provider regardless of the key: claude | local (blank = auto)
AI_PROVIDER=
