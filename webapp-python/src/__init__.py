"""Job Organizer (Python port) — Option C.

A Python/Streamlit port of the PHP web-app prototype's best features:
a rich category schema, code-enforced guardrails (including date-grounding),
a no-API-key rule-based fallback, a timeline/summary aggregator, and a
field-by-field accuracy scorer. Defaults to Anthropic Claude.
"""

__version__ = "1.0.0"
